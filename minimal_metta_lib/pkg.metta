; The manifest for minimal_metta_lib: what it depends on, and where its
; source is. The library itself is lib.metta beside this file.

!(import! &self (library lib_import))

!(import! &self (library "minimal_metta_lib/lib.metta"))
